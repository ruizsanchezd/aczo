import { Meta } from "@storybook/addon-docs/blocks";
import { Text } from "../../ui/Text";
import { ModoClaro, spacing } from "../TokenSwatch";

<Meta title="Fundamentos/Espaciados" />

<ModoClaro>

# Espaciados

La escala de espaciado va de `00` a `10`. En clases se usan como **p-04**, **gap-06**,
**mt-02**… La sección (**layout-section**) es 600px de ancho máximo con 16px de padding en
móvil, y 1200px con 32px en desktop.

<div className="flex flex-col gap-03">
  {/* Los anchos de columna van a pelo: son el andamio de esta tabla de
      documentación, no valores de diseño del Figma. La barra sí usa el
      token: su ancho ES el valor que describe. */}
  {spacing.map(([token, px]) => (
    <div key={token} className="flex items-center gap-04">
      <Text variant="body-s" color="mid" className="w-[110px] shrink-0">
        layout-size-{token}
      </Text>
      <Text variant="body-s" color="low" className="w-[48px] shrink-0">
        {px}px
      </Text>
      <div className="h-04 bg-highlight-vivid" style={{ width: `${px}px` }} />
    </div>
  ))}
</div>

</ModoClaro>
